services:
  - type: web
    name: cognitive-load-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn live_cbas.server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
